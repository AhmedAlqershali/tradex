.env
.env.*
!.env.example
.git
.github
vendor
node_modules
public/build
# Keep committed files on the public disk in the image so the public/storage
# symlink can serve existing avatars, logos, and product images. Runtime
# directories and private uploads remain excluded and are recreated/mounted by
# the container at startup.
storage/framework
storage/logs
storage/app/private
database/*.sqlite
database/*.sqlite-*
bootstrap/cache/*.php
tests
Dockerfile
compose.yaml