@extends('admin.layout')

@section('title', 'Product · '.$product->name)
@section('heading', 'Product details')

@section('content')
    <div class="mb-6"><a href="{{ route('admin.products.index') }}" class="text-sm font-semibold text-indigo-700 hover:text-indigo-900">← Back to products</a></div>
    <section class="max-w-4xl overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm shadow-slate-200/40"><div class="border-b border-slate-100 px-6 py-5"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-wider text-slate-400">Product #{{ $product->id }}</p><h2 class="mt-1 text-xl font-semibold text-slate-900">{{ $product->name }}</h2></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize text-slate-600">{{ str_replace('_', ' ', $product->status) }}</span></div></div><dl class="grid gap-5 px-6 py-6 sm:grid-cols-2"><div><dt class="text-xs font-semibold uppercase tracking-wider text-slate-400">Store</dt><dd class="mt-1 font-medium text-slate-800">{{ $product->store?->store_name ?? 'Store unavailable' }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wider text-slate-400">Category</dt><dd class="mt-1 font-medium text-slate-800">{{ $product->category?->name ?? 'Uncategorized' }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wider text-slate-400">Price</dt><dd class="mt-1 font-medium text-slate-800">{{ number_format((float) $product->price, 2) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wider text-slate-400">Stock</dt><dd class="mt-1 font-medium text-slate-800">{{ $product->quantity }}</dd></div><div class="sm:col-span-2"><dt class="text-xs font-semibold uppercase tracking-wider text-slate-400">Description</dt><dd class="mt-1 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $product->description ?: 'No description provided.' }}</dd></div></dl><div class="border-t border-slate-100 px-6 py-5"><p class="text-sm font-medium text-slate-700">Admin product changes are not available because the existing admin service intentionally exposes monitoring only.</p></div></section>
@endsection